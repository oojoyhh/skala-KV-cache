# Python 3.10 이상 · 가상환경(.venv)에서 설치: pip install -r requirements.txt
# 버전은 12:00 1차 통합 때 통합 환경에서 pip freeze로 고정한다 (docs/DEV_PLAN.md §4)

# 그래프 · LLM
langgraph
langchain-core
langchain-openai
python-dotenv
pydantic

# RAG (1·2번) — 임베딩 BAAI/bge-m3
langchain-community
langchain-huggingface
sentence-transformers
faiss-cpu
pymupdf

# 웹 검색 (3·4번)
tavily-python

# 보고서 PDF (6번)
fpdf2>=2.7          # pdf.table 사용 (테스트 버전 2.8.8)

# 테스트
pytest